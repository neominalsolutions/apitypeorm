// Const {createClient} = require('redis');

// const redisClient = await createClient({url: '127.0.01:6379'})
// 	.on('error', err => console.log('Redis Client Error', err))
// 	.connect();

// module.exports = redisClient;
